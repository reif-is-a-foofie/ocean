# dyno game - Deployment Configuration
# Generated by OCEAN using Codex MCP

deployment:
  platform: render
  type: web
  name: dyno-game
  env: python
  buildCommand: pip install -e .
  startCommand: python app.py
  envVars:
    - key: PYTHON_VERSION
      value: 3.11
  healthCheckPath: /healthz
  autoDeploy: true
